# DICTAMEN RONDA 3 — correccion + inserciones T5

## VEREDICTO
**VERDE.** Cero GRAVE vivo: la corrección C1–C5 cierra lo que dice sin duplicar ni perder valor en operación normal, y las inserciones T5, desarmadas, no cambian los valores por defecto del camino del dinero.

## (A) LA CORRECCION

### [MEDIO] C5 spawnea primero y descuenta después: un crash con persistencia de las pilas nuevas deja BTC en el suelo y stock intacto
- **Donde:** `scripts/4_World/LFPG_BTCAtm.c:316-354` (spawn + `SetQuantity` + `drops.Insert`) y `scripts/4_World/LFPG_BTCAtm.c:357-374` (`LFPG_RemoveBtcStock` solo si `remaining != stockBefore`)
- **Que pasa:** el orden es CreateObjectEx → insertar en `drops` → luego `LFPG_RemoveBtcStock(delivered)`. No hay transacción durable conjunta. `m_BtcKillDropHandled` se pone a true al entrar (`:285-287`) y no se persiste (`:47`, ausente de `LFPG_OnStoreSaveExtra` en `:392-397`).
- **Escenario de fallo:** (1) ATM con `m_BtcStock > 0`. (2) `EEKilled` → `LFPG_OnKilled`. (3) Al menos una pila ya existe en el mundo. (4) El proceso muere antes de que `LFPG_RemoveBtcStock` persista el descuento. (5) El siguiente boot restaura el ATM con el stock viejo **y** las pilas nuevas si el almacenamiento llegó a escribirlas. Hace falta crash + escritura de esas entidades nuevas + stock del ATM aún sin descontar. No es operación normal; no lo subo a GRAVE.
- **Correccion propuesta:** no cabe en esta ronda (invertir el orden falla hacia pérdida: stock ya descontado y spawn a medias). Archivar: residual de C5, misma familia que una ventana de crash, no un dup en vivo.
- **Confianza:** CONFIRMADO el orden y que el descuento va después; PLAUSIBLE el dup tras crash (depende del orden real del hive, no medido)

### VEREDICTO POR CORRECCION
- **C1 — CIERRA.** Las cinco líneas se recortan con `Trim()` en `scripts/3_Game/LFPG_FileUtil.c:619-624` después de `CloseFile` y antes de comparar UID/classname o `ParseNonNegativeIntText`. Firma abierta: `proto string Trim()` en `scripts/1_core/proto/enstring.c:304` (contrato `:292-300`). Si `FGets` deja `\r`, esto es el arreglo pedido; si no lo deja, `Trim()` de una línea limpia es identidad. No empeora el fail-closed de lectura incompleta (`:616-617`).
- **C2 — CIERRA.** El default sigue `return false` en `scripts/4_World/lfpg_devicebase.c:615-618`; el comentario de `:610-613` ya no habla de fail-closed. Overrides: `scripts/4_World/LFPG_Battery.c:122-125` (`m_StoredEnergyX10 > 0`, en `LFPG_BatteryBase`, lo heredan Medium y Large) y `scripts/4_World/LFPG_Furnace.c:101-104` (`m_FuelCurrent > 0`). `LFPG_ValidateDismantle` consulta el guard en condición y al terminar (`scripts/4_World/LFPG_ActionDismantleDevice.c:116-117` y `:162`).
- **C3 — CIERRA.** Si `destroyed != btcAmount`, error + `return` en `scripts/5_Mission/LFPG_BTCHelper.c:1523-1534` sin `ClearSellDestroyIntentAfterDestroy`. Solo la destrucción completa llega a `:1536`. La marca de boot se pone antes (`:1521`). Mismo boot: la guarda `:1512-1516` impide re-destruir. Tras reinicio el marcador sigue armado (documentado; misma familia que el MEDIO de clear+rebase+reinicio ya aceptado). No empeora el camino del dinero en el mismo proceso.
- **C4 — CIERRA.** Los cinco `ref` citados ya no están: `scripts/3_Game/LFPG_BTCConfig.c:485-486`, `:501`; `scripts/5_Mission/LFPG_BalanceProvider_NativeImpl.c:1987` y `:2108` son `LFPG_BalanceData data = new …` sin `ref`. `DestroyPlayerCash` no aparece en `scripts/`. Queda un `ref` local previo en `:2121` (`ref LFPG_BalanceEntry entry`); no era de los cinco y no lo relitigo.
- **C5 — CIERRA.** Drop en `LFPG_BTCAtmBase.LFPG_OnKilled` (`scripts/4_World/LFPG_BTCAtm.c:282-381`), `#ifdef SERVER`, tope 64 (`:45`, `:309`). El ATM jugador llama `super.LFPG_OnKilled()` en `:567` y luego apaga el LED. El admin no overridea `LFPG_OnKilled`; hereda el drop. Stock 0 sale en `:285-286`. El descuento pasa por `LFPG_RemoveBtcStock` (`:363`), no por el setter que recorta al cap. Respuestas a las preguntas del brief:
  - **¿Compila?** No lo compilé. Revisión estática: cero ternarios / `foreach` / `++` / `+=` en el cuerpo nuevo; `CreateObjectEx` / `ObjectDelete` / `ECE_CREATEPHYSICS` / `SetQuantity` / `GetQuantityMax` / `HasQuantity` / `Math.RandomFloat` / `GetPosition` abiertos en vanilla; el patrón `new array<EntityAI>()` ya existe en `scripts/5_Mission/LFPG_BTCHelper.c:429` y `:450`; World no llama a Mission. No vi error de tipado. Un fallo real al cargar el mundo seguiría siendo GRAVE; no lo tengo.
  - **`if (!item)` después de `SetQuantity`:** no es use-after-free en este call. Vanilla `ItemBase.SetQuantity` (`scripts/4_world/entities/itembase.c:3339-3408`) con `(qty, false, false)` no entra en `this.Delete()`. Aunque entrara, `Object.Delete()` (`scripts/3_game/entities/object.c:82-85`) encola `ObjectDelete` al frame siguiente; el wrapper script sigue vivo **en este** frame, así que `if (!item)` en `:342` no detecta el borrado. El bool de `SetQuantity` es “¿lo borré?”, no éxito. El mismo `(false, false)` ya lo usa `LFPG_ConsumeFromStack` (`scripts/5_Mission/LFPG_BTCHelper.c:370`). Comprobar `if (!item)` es válido en Enforce; aquí es un no-op defensivo, no un UAF.
  - **`array<EntityAI> drops` sin `ref`:** los locales Managed son fuertes hasta el fin de ámbito (`enforce-script-reference` / memoria Enforce). El array vive todo el hook. No hace falta que sobreviva después: las entidades las sostiene el mundo. Convención de la casa: `ref` solo en miembros. Correcto.
  - **`m_BtcKillDropHandled` no persistido / dos instancias:** `EEKilled` en `scripts/4_World/lfpg_devicebase.c:273-283` llama `LFPG_OnKilled` **antes** de `super.EEKilled`. Vanilla `EntityAI.EEKilled` (`scripts/3_game/entities/entityai.c:1078-1087`) no borra el objeto; `ReplaceOnDeath` default es false. `GetPosition()` en `:304` corre sobre un ATM arruinado aún en el mundo. Un `ObjectDelete` directo (destornillador) no pasa por `EEKilled`; T6 ya lo bloquea si hay stock. Tras un boot, una instancia nueva no vuelve a disparar `EEKilled` por estar ya ruinada. No vi un camino de dos instancias distintas del mismo ATM que re-tiren el drop sin un kill nuevo.
  - **Rollback si `LFPG_RemoveBtcStock` falla:** `ObjectDelete` es `proto native void` (`scripts/3_game/global/game.c:704`), inmediato, no el `Delete()` diferido. Si el native no borra, quedaría suelo + stock: no hay evidencia de que `ObjectDelete` sobre un spawn fresco falle; no lo marco GRAVE.
  - **Orden spawn→descuento / crash:** el MEDIO de arriba. Fail hacia dup en ventana de crash, no hacia pérdida.
  - **`EEKilled` y borrar al momento:** `LFPG_DeviceLifecycle.OnDeviceKilled` (`scripts/4_World/LFPG_DeviceLifecycle.c:35-47`) solo corta cables; no toca stock ni claims. El drop corre con el objeto aún registrado, así que `PrepareStockMutation` puede aceptar.

## (B) INSERCIONES DE T5

Este árbol (`fix/t7-correcciones-revision`, `9d2d06a`) **no contiene** `LFPG_FaultInject` ni las inserciones. Lo de abajo es el diff `reviews/2026-09-08-t1-t6-revision-cruzada/T5-INSERCIONES-CAMINO-DINERO.diff` anclado a las líneas **actuales** del destino.

### [MEDIO] Con el inyector armado, `ShouldCrash` después de borrar el target de balances deja el JSON de saldos ausente y aborta el save
- **Donde:** ancla actual `scripts/3_Game/LFPG_FileUtil.c:270-271` (`DeleteFile(targetPath)` luego `CopyFile`); inserción en `T5-INSERCIONES-CAMINO-DINERO.diff` (hunk `balances_after_delete_before_promote`)
- **Que pasa:** el diff inserta `if (ShouldCrash("balances_after_delete_before_promote")) return false;` **después** de borrar el target y **antes** de promocionar el `.tmp`. Desarmado (`ShouldCrash` falso) el flujo es el de hoy. Armado, el caller ve `false`, revierte RAM, y en disco puede no haber target hasta el restore del `.bak`.
- **Escenario de fallo:** hace falta el JSON del harness **y** frase de armado **y** el escenario que dispara ese id. No es un servidor de producción sin ese fichero. No es GRAVE del camino desarmado.
- **Correccion propuesta:** no tocar el camino del dinero. El harness tiene que vivir fuera de producción (no desplegar el JSON / no fusionar el inyector a un PBO que vaya a jugadores). Si se fusiona el código, el riesgo es el fichero, no la forma `if (!ShouldFail) …` del resto de hunks.
- **Confianza:** CONFIRMADO el orden delete→crash-hook→promote en el diff; PLAUSIBLE el daño si alguien arma el escenario; no CONFIRMADO que `ShouldCrash` en producción pueda ser true (no abrí `LFPG_FaultInject.c` en este árbol)

No hay GRAVE en las inserciones **desarmadas**. Respuestas pedidas:

1. **¿Puede `ShouldFail` / `ShouldCrash` ser true en producción?** En este worktree el inyector no está. No abrí `ShouldFail`/`ShouldCrash`. Lo que sí está en código/diff: todas las ramas de dinero son `if (predicado) saltar/return` con el predicado en corto-circuito hacia el comportamiento viejo cuando es falso. `bool cleared = false` / `bool promoteOk = false` solo se asignan si `!ShouldFail`; desarmado es `cleared = Clear…` y `promoteOk = CopyFile…`. Medido en el gate de T5 (HANDOFF, no lo re-ejecuté): `[ERR] LFPG_FAULTINJECT DISARMED reason=no_file`. Ese log implica fichero ausente → desarmado. Un JSON que aparezca después, o un parse a medias, **no lo pude verificar** en el cuerpo del inyector. No lo invento como GRAVE.
2. **¿Alguna rama deja el dinero peor con T5 desarmado?** No en los wrappers de valor. `cleared`/`promoteOk`/`rebased`/`toAdd` no cambian si el predicado es falso. `ObserveTx` / `ObserveReconcile` / `ShouldCrash("E16_crash_after_apply")` (retorno descartado) son llamadas extra; sin el inyector no puedo probar que `Touch()`/`Observe*` no escriban saldos, pero el diff no altera asignaciones de dinero en esas líneas. `AddBalance_partial` solo recorta `toAdd` si `ShouldFail` es true.
3. **¿Choque con C1/C3?** C1 (`TryReadSellDestroyIntent` `:619-624`) no está en el diff T5. C3 pone `return` en destrucción parcial (`:1523-1534`) y no llama a `ClearSellDestroyIntentAfterDestroy`. T5 envuelve ese Clear/rewrite **después** de una destrucción completa y observa el reconcilio. Semántica compatible **si al fusionar se conserva el `return` de C3**. El hunk T5 de `ObserveReconcile` + `ClearSellDestroyIntentAfterDestroy` está contra el árbol pre-C3 (`Error(shortMsg);` y luego Clear siempre). Un apply mecánico que tire el `return` reabre C3.

## LO QUE HE VERIFICADO
- Árbol vivo de C1–C5: `LFPG_FileUtil.c` (Trim + `ParseNonNegativeIntText`), `lfpg_devicebase.c` (comentario + `EEKilled`/`EEDelete`), `LFPG_Battery.c`, `LFPG_Furnace.c`, `LFPG_ActionDismantleDevice.c` (condition + finish), `LFPG_BTCAtm.c` (drop + player `super` + admin sin override), `LFPG_BTCHelper.c` (C3, `DestroyPlayerItems`, sell a cuenta, `ClearSellDestroyIntentAfterDestroy` sin T5), `LFPG_BTCConfig.c` y `LFPG_BalanceProvider_NativeImpl.c` (C4 + `PrepareStockMutation` `:519-599` + orphan `:1412-1542`).
- Vanilla: `SetQuantity` / `HasQuantity` / `GetQuantityMax` (`itembase.c:3339-3506`), `EntityAI.SetQuantity` declarado (`entityai.c:2242`), `Delete` diferido (`object.c:82-85`, `entityai.c:774-778`), `ObjectDelete`/`CreateObjectEx` (`game.c:702-704`), `ECE_CREATEPHYSICS` (`centraleconomy.c:16`), `Trim` (`enstring.c:304`), `EEKilled` (`entityai.c:1078-1087`).
- `LFPG_DeviceLifecycle.OnDeviceKilled` no toca stock. Claims huérfanos corren cuando el ATM ya no está en el registry (`EEDelete`), después del drop.
- `maxBtcPerMachine` default 100 (`LFPG_BTCConfig.c:68`). Tope 64 entidades: con pilas de 25 cabe; con `maxStack=1` un ATM lleno deja residual en el aparato arruinado (mejor que perderlo todo, que era el estado pre-C5).
- T5: leí el diff entero de inserciones. Grep en este árbol: cero `LFPG_FaultInject`/`ShouldFail`/`ShouldCrash`. Rama `feat/t5-harness` existe en `.git/refs/heads` (`1f12ac0`); no la desmonté.

## LO QUE NO HE PODIDO VERIFICAR
- Compilación Enforce de C5 (ni AddonBuilder ni carga de mundo).
- Cuerpos de `ShouldFail` / `ShouldCrash` / `Touch` / parse del JSON (`LFPG_FaultInject.c` no está en este worktree). Si un JSON accidental o a medias arma el inyector, no lo confirmé en fuente.
- Round-trip `FPrintln`→`FGets` con CRLF: si `Trim()` trata `\r` como whitespace (el ejemplo vanilla solo muestra espacios).
- In-game: muerte por daño de ambos ATM, jitter/física, `ObjectDelete` sobre spawn fresco, hive entre spawn y `OnStoreSaveExtra`, `GetQuantity` vs `GetAmmoCount` en el `btcItemClassname` de producción (el ATM ya usa el mismo `SetQuantity` al retirar; C5 copia ese patrón).
- Que el merge T5+C3 conserve el `return` de C3.