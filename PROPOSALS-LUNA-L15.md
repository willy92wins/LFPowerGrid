# Propuestas de reduccion LOC — Luna L15

Base revisada: `scripts/4_World/LFPG_BTCAtmController.c` (1027 líneas en `033c08c`). Inventario previo a edición; LOC ahorradas son estimaciones de borrado neto y no cuentan líneas en blanco/comentarios si no se indica.

| Prioridad | Propuesta | Ahorro estimado | Decisión |
|---|---|---:|---|
| 1 | Consolidar los cuatro cuerpos RPC de mutación (`SendBTCRpc`, `SendBTCSellRpc`, `SendBTCBuyRpc`, `SendBTCCashRpc`): comparten adquisición de jugador, `BeginMutation`, refresh de sesión y envío. Un emisor común con payload condicional conservaría la secuencia exacta de `Write`; los campos de cuenta solo se emiten para buy/sell. | 45–60 | Diferida: reescritura significativa de protocolo sensible; requiere probar equivalencia del orden y tipos serializados. |
| 2 | Unificar `GetBtcInput` y `GetEurInput` en un lector de `EditBoxWidget`; mismo manejo de widget ausente, texto vacío, `ToInt` y negativos. | 8–12 | Diferida: helper adicional con parámetro; pequeño ahorro neto con cambio de ruta compartida de entrada. |
| 3 | En `RequestBTCSession`, eliminar `if (!player) return`: todas las llamadas actuales llegan desde los emisores tras validar `player`. | 2 | Diferida: método `protected` y su contrato futuro permitirían otros llamadores; no es comprobablemente redundante bajo el alcance del archivo. |
| 4 | Quitar `#ifndef SERVER` dentro de cada emisor RPC: el archivo completo ya está dentro de `#ifndef SERVER` (línea 1). | 8 | Aplicada: los cuatro bloques eran redundantes porque el archivo ya empieza bajo `#ifndef SERVER`; 8 líneas borradas. |
| 5 | Retirar comentarios de cabecera que describen sprint/historial y la lista de cambios ya incorporados; también comentarios de sección repetitivos. | ~45–65 | Diferida: LOC no funcional, pero baja la mantenibilidad/documentación; mantener las notas con contrato e invariantes monetarios. |
| 6 | Reducir inicializaciones del constructor (`m_AccountMode`, guards, timer y `m_View`) si el runtime de Enforce garantiza inicialización cero de todos esos campos. | 5 | Diferida: no se verificó aquí el contrato del lenguaje/runtime; eliminar puede cambiar inicialización observada. |
| 7 | Eliminar clamp de negativos de los dos lectores porque los seis handlers rechazan cantidades `<= 0`. | 8 | Diferida: los métodos de lectura son `protected`; el valor negativo observable puede afectar a subclases/llamadores y se transforma a cero hoy. |
| 8 | Eliminar `if (decPart < 0)` al formatear variación tras usar `absChange`. | 4 | Diferida: podría considerarse inalcanzable, pero float/overflow/conversión deben probarse para afirmar equivalencia en todos los valores de entrada. |
| 9 | Simplificar ramas de color `isError` y `dimmed` mediante selección de valor antes de una única llamada por widget. | 6–10 | Diferida: reescritura, no borrado; no es un recorte conservador sin confirmar restricciones de expresiones del dialecto. |
| 10 | Consolidar ramas repetidas de éxito BTC y EUR en `BuildTxMessage` usando variable de clave/texto compartida; los cuatro mensajes BTC concatenan el mismo sufijo. | 20–30 | Diferida: mismo patrón requiere una tabla/selector y reescribe el formateo; traducción y orden de concatenación se deben conservar exactamente. |
| 11 | Consolidar las dos ramas cash success de `BuildTxMessage`; ambas redondean el mismo importe y añaden ` E`. | 8–12 | Diferida: misma cautela de traducción/flujo de control; ahorro posible pero exige reescritura. |
| 12 | Reducir cada traducción de error en `BuildTxMessage` de tres sentencias a una llamada directa en `return`. | ~28–35 | Diferida: comportamiento probablemente idéntico, pero sería reescritura de 13 casos; hacer después de comprobar el contrato de `TranslateString` (sin efectos laterales). |
| 13 | Sustituir variables locales triviales usadas una sola vez (`subId`, `netLow`, `netHigh`, algunas claves/textos) por expresiones directas. | ~25–40 | Diferida: conjunto de microcambios de reescritura, mezcla llamadas con valores de retorno; no todos son redundantes sin inspección del evaluador. |
| 14 | Quitar las comprobaciones `if (m_EditBtc/m_EditEur)` interiores tras validar el mismo widget al principio de conversión. | 2–4 | Diferida: la llamada a `SetText` puede disparar callbacks/reentrada; retener el guard local protege esa frontera. |
| 15 | Quitar `FormatEurInt` y concatenar el formato en sus dos consumidores. | 0–2 netas | Parcialmente aplicada: en ambos métodos eliminé la variable `suffix` y concatené el mismo literal directamente (2 líneas netas ahorradas). Mantener dos helpers sigue sin ser una reducción neta. |
| 16 | Eliminar `ShowTxFeedback`, `TickTimers`, `InitWidgetRefs`, `ResetTxState`, `OnBtcAmountChanged`, `OnEurAmountChanged`, o helpers de formato por parecer auxiliares/no usados internamente. | hasta ~150 | Descartada: tienen llamadores en `LFPG_BTCAtmView.c` o el comportamiento de conversión/reset depende de ellos; no son código muerto. |
| 17 | Eliminar guardas `if (m_*Widget)` de actualización/estado por haberse enlazado los widgets. | variable | Descartada: `InitWidgetRefs` permite refs ausentes y las comprobaciones evitan acceso inválido. |

No se detectaron duplicados de campos, funciones privadas completamente huérfanas ni bloques funcionales inalcanzables con evidencia suficiente para borrarlos directamente. Los únicos duplicados grandes son candidatos a consolidación con riesgo de protocolo o semántica y quedan diferidos en esta ronda.
